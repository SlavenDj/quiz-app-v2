services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: quiz_db
    ports:
      - "3307:3306"
    volumes:
      - quiz-mysql:/var/lib/mysql
volumes:
  quiz-mysql:
