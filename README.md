# Quiz App v2 — rebuild

Same product as `../Quiz-app`, architected right.

- Backend: Express + Prisma + Zod (`apps/api`)
- Frontend: Vite React TS + TanStack Query (`apps/web`)
- DB: SQLite locally (`DB_PROVIDER=sqlite`), MySQL on cPanel (`DB_PROVIDER=mysql`)

See `apps/api/.env.example` + `apps/web/.env.example`.
Phase plan in opencode conversation (Phases 1-5).
